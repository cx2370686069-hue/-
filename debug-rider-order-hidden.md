# [OPEN] rider-order-hidden

## 问题现象
- 商家端接单后，骑手端可以听到新订单语音提示。
- 但骑手端订单列表里看不到这笔订单。

## 当前目标
- 用运行时证据确认：订单到底有没有进入后端列表结果。
- 用运行时证据确认：如果后端返回了，前端是在哪一层把它过滤掉了。

## 可证伪假设
1. `getRiderOrders(骑手订单列表接口)` 已经返回了订单，但前端二次过滤挡掉了。
2. 实时推送和列表查询的骑手身份口径不一致，导致“能播报、查不到”。
3. 订单被提前写入了归属字段，仍然能播报，但不再属于待接单池。
4. 骑手端页面实际调的接口或当前 tab(标签页) 不是预期那一套。

## 下一步
- 先做最小化插桩，只打运行时日志，不改业务判断。
- 先采集一次“商家接单后，骑手端听到语音但列表没单”的完整证据。
