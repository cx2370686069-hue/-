# [OPEN] confirm-delivery-network-error

## 背景
- 现象：骑手点击“确认送达”后，大约 15 秒出现“网络错误，请检查网络”。
- 期望：确认送达成功后只刷新正确状态，不出现错误提示。

## 当前假设
- A. 确认送达后，骑手端自动刷新“配送中订单列表”接口，该接口在 15 秒左右超时并弹出网络错误。
- B. 确认送达接口本身成功，但后续某个详情/轮询请求失败，前端统一提示成网络错误。
- C. 骑手端请求封装或页面逻辑对相关请求设置了约 15 秒超时，导致稳定复现。
- D. H5 地图页和外层 uni-app 页面之间的消息联动触发了额外请求或重复刷新。

## 调试计划
1. 先给后端确认送达链路和相关列表接口加运行时日志，不改业务逻辑。
2. 复现一次，读取调试日志，对照确认是哪个接口在 15 秒附近失败。
3. 只在证据明确后再做最小修复。
