# [OPEN] rider-orders-network-error

## 现象
- 骑手端在 `pages(页面)/orders(订单)/index.vue(订单列表页)` 里，多次执行“进入订单 -> 返回 -> 进入 -> 返回”后，界面会弹出“网络错误，请检查网络”。
- 当前用户反馈：同步后重新运行，重复进入退出几次后仍可稳定触发。
- 当前截图显示的可疑点包括：
  - `pages/orders/index.vue` 加载订单失败
  - `utils/reminder-center.js:78` 轮询报错
  - 终端里存在 `request:fail timeout` / `code:500,msg:网络错误` 一类信息

## 预期
- 订单页频繁进入退出时，不应出现“网络错误，请检查网络”。
- 即使后台轮询偶发失败，也不应该影响前台订单页反复弹错。

## 本轮假设
- H1：订单列表页在进入/返回切换时，仍然触发了重复请求，多个并发请求中有一部分超时。
- H2：请求已经从骑手端发出，但后端 `/order/rider-orders` 没有在客户端超时前返回。
- H3：提醒中心轮询和订单页前台请求撞在一起，导致同一时间内对 `/order/rider-orders` 压力过高。
- H4：实际报错并非只来自 `/order/rider-orders`，而是某个相邻请求失败后被统一包装成“网络错误”。
- H5：骑手端 `request(请求封装)` 的失败分支缺少关键信息，导致我们现在只能看到“网络错误”，看不到失败前的真实上下文。

## 当前策略
- 第一步：只加运行时取证日志，不改业务逻辑。
- 第二步：让用户按“进入订单 -> 返回 -> 进入 -> 返回”复现一次。
- 第三步：读取日志，确认到底是“没发出去”、“发出去了但超时”、“被别的请求拖垮”中的哪一种。

## 当前状态
- 状态：已完成前后端插桩，等待下一次真实复现

## 新观察
- 用户反馈：每次同步后重新运行，短时间内看起来正常；但过几十分钟或几小时后，或者多次进入/返回订单页后，问题又会出现。
- 这说明问题更像“长期运行后的状态累积 / 重复请求叠加”，不像单次固定输入就必现的业务判断错误。

## 当前插桩位置
- `骑手端(前端)/utils/request.js(请求封装)`：记录 `/order/rider-orders` 的发起、成功、失败、耗时、当前路由
- `骑手端(前端)/pages/orders/index.vue(订单列表页)`：记录 `onLoad/onShow/orderRefresh/loadOrderList` 的触发顺序
- `后端(服务端)/controllers/orderController.js(订单控制器)`：记录 `getRiderOrders` 是否进入、是否成功返回、是否报错
