[OPEN] 云端 `/api/map/reverse-geocode` 返回 404 调试记录

## 现象
- 用户直接访问 `http://121.43.190.218:3000/api/map/reverse-geocode?lat=32.1989548569&lng=115.66562348`
- 云端返回：`{"code":404,"message":"接口不存在"}`

## 目标
- 以云端实际访问结果为准，确认新接口是否已在公网服务生效
- 找出 404 的真实原因：未同步、未重启、挂载遗漏、运行目录错误，或公网访问不是当前代码

## 初始假设
1. 云端服务器还没同步到新增 `routes/map.js` 的版本
2. 云端代码已同步，但 Node 服务未重启，仍在跑旧进程
3. 云端实际运行的项目目录不是当前本地这份仓库
4. 总路由或 `/api` 前缀在云端版本里没有生效
5. 公网 `121.43.190.218:3000` 指向了另一套服务，而不是当前待部署后端

## 当前限制
- 目前只掌握公网访问结果和本地代码
- 尚未直接读取云端服务器文件系统或进程状态
