# Debug Session: rider-hidden-order [OPEN]

## 问题现象
- 用户下单后，商家端点击接单。
- 骑手端能收到新订单语音提醒。
- 骑手端“未接单”列表没有出现订单。
- 最近一次还出现过 `WHERE parameter "rider_id" has invalid "undefined" value` 这类后端查询报错。

## 期望结果
- 商家接单后，符合条件的骑手端应同时收到语音提醒和订单列表数据。

## 当前调试范围
- 后端 `controllers(控制器)/orderController.js(订单控制器)` 的骑手订单列表接口
- 后端 `services(服务)/socketService.js(Socket服务)` 的新单语音推送
- 骑手端 `pages(页面目录)/orders(订单页)/index.vue(订单列表页)` 的订单加载与筛选

## 可证伪假设
1. 语音推送命中了“乡镇在线骑手池”，但列表接口 where 条件把订单过滤掉了。
2. 订单列表接口返回了这笔订单，但骑手端页面在本地筛选 tab 时把它筛没了。
3. 订单在“播语音”和“拉列表”之间状态发生变化，导致语音口径和列表口径不一致。
4. 订单乡镇字段、责任人字段或配送身份字段有一项和骑手登录身份不一致，导致只推送不展示。

## 计划
1. 先给后端列表接口和 Socket 推送点加运行时日志，记录订单、骑手、where 条件和状态。
2. 让你复现一次“商家接单后骑手只听到语音”的流程，收集 pre-fix 证据。
3. 根据日志排除假设后，再做最小修复。

## 当前状态
- 已建立调试会话。
- 已在后端 `controllers(控制器)/orderController.js(订单控制器)` 增加列表入口、where 条件、返回结果、异常日志。
- 已在后端 `services(服务)/socketService.js(Socket服务)` 增加乡镇新单语音推送日志。
- 已在骑手端 `pages(页面目录)/orders(订单页)/index.vue(订单列表页)` 增加请求前、接口返回后、本地筛选后、刷新事件日志。
- 当前调试服务实际监听地址是 `http://192.168.1.9:7788/event`，插桩上报端口已统一改到 `7788`。
- 已拿到 `pre-fix` 证据：
  - 骑手端订单页收到 `new_order` 刷新事件，说明提醒链路确实到了。
  - 骑手端随后请求订单列表，接口返回 `count=0`，不是前端本地筛选把单筛没。
  - 当前最符合证据的根因是：实时推送按“归一化乡镇名”匹配，订单列表查询却还按 `customer_town === scope.town_name` 原始字符串精确匹配。
- 已开始最小修复：统一 `src(源码主目录)/domains(领域模块)/delivery(配送领域)/policies(策略)/order-visibility.policy.js(订单可见范围策略)` 的乡镇匹配口径，让它和实时推送保持一致。
