[OPEN] 订单下单后商家端未收到订单调试记录

## 这个文件是干什么的

这次是排查“用户端下单后，商家端没有收到订单”的专用调试记录。
这里先不急着改业务逻辑，先把假设、证据、结论、后续动作按顺序记清楚，避免凭感觉乱改。

## 本次问题现象

- 用户端发起下单
- 商家端没有看到新订单，或没有收到新订单提示
- 目前还不确定问题发生在用户端、后端，还是商家端

## 当前排查目标

- 先确认用户端有没有真正把“创建订单/支付成功”请求发到后端
- 再确认后端有没有真正创建订单、把订单推进到商家可见状态
- 最后确认后端有没有给商家端发通知，以及商家端有没有正常接收

## 初始假设

- 假设 1：用户端下单请求根本没有发到后端，或者请求失败但前端没提示清楚
- 假设 2：用户端创建了订单，但支付成功后订单状态没有推进到商家待接单状态
- 假设 3：后端订单已经创建成功，但没有触发给商家端的 WebSocket 或推送通知
- 假设 4：后端已经把通知发出去了，但商家端没有正确建立连接、监听事件，或者过滤掉了消息
- 假设 5：商家端实际能拉到订单列表，只是“新订单提示”这一层坏了，看起来像没收到订单

## 当前状态

- 状态：排查中
- 业务逻辑改动：未开始
- 证据收集：已做静态链路核对，运行时证据待补

## 当前已拿到的代码证据

- 证据 1：用户端 `pages/cart/checkout.vue` 下单成功后，只是跳转到 `pages/pay/index.vue(支付页)`，并没有在创建订单时直接让商家看到订单
- 证据 2：后端 `controllers/orderController.js` 的 `createOrder()` 创建订单时，默认把订单写成 `status=0`
- 证据 3：后端只有在订单已经变成 `status=1` 时，才会调用 `socketService.notifyMerchantNewOrder()` 推给商家
- 证据 4：用户端支付页 `pages/pay/index.vue` 走的是旧接口 `/order/pay`
- 证据 5：后端 `controllers/orderController.js` 的 `payOrder()` 现在只创建预支付流水，返回 `payment_status=pending`，没有直接把订单改成 `status=1`
- 证据 6：用户端支付页在 `mode=mock` 时，会继续调用 `/pay/mock/confirm`
- 证据 7：后端 `modules/payment/routes.js` 里目前只看到 `/pay/prepay`、`/pay/wechat/notify`、`/pay/alipay/notify`，没有看到 `/pay/mock/confirm`
- 证据 8：商家端订单页和兜底轮询都把“新订单”口径定死在 `status=1`，如果订单停在 `status=0`，商家端列表和提醒都会一起沉默
- 证据 9：用户端 `config/index.js` 和商家端 `config/index.js` 当前都直连云端 `http://121.43.190.218:3000`，不是本地 `E 盘` 后端
- 证据 10：用户端 `utils/request.js` 只把 `statusCode === 200` 当成功，但后端 `createOrder()` 返回的是 `201`
- 证据 11：用户端结算页虽然对 `201` 做了“看起来像成功”的兜底，但请求封装在 reject 时已经把订单实体丢掉了，后续拿不到有效 `orderId`

## 基于当前证据的阶段性判断

- 用户端“创建订单”大概率不是主问题
- 商家端“接收提醒”大概率也不是主问题
- 目前最可疑的断点在“支付闭环没有真正确认成功，导致订单一直停在待支付”
- 只要订单还停在 `status=0`，后端就不会推给商家，商家端轮询也拉不到这笔新订单
- 另外还存在一条很实锤的前端问题：用户端把后端返回的 `201(创建成功)` 当成失败分支处理，这会让下单结果展示和后续跳支付页都发生错乱
- 当前本地调试日志没有生成，不能直接说明“请求没发出去”，更大的可能是因为前后端实际都在打云端，而不是打本地调试环境

## 下一步

- 去补最小调试点，抓一次真实下单到支付后的运行时日志
- 优先验证：`/order/pay` 后订单有没有从 `0 -> 1`
- 再验证：后端有没有真正发出 `new_order`，商家端有没有收到并转成页面刷新事件
